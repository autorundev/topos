
import { DataArtifactDefinition } from '../types';

// Placeholder — Vector data-артефакты будут наполнены позже (write, event, starter, edge…).
export const DATA_ARTIFACTS: DataArtifactDefinition[] = [];

import { WorkflowTemplate } from '../types';

// Placeholder — единый холст «tmpl_vectoros» (backbone + веер входов/выходов + петля
// effect→write) собирается визуально в билдере позже. Пока карту несут EXAMPLES.
export const WORKFLOW_TEMPLATES: WorkflowTemplate[] = [];
